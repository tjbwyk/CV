for i = 0 : 99
    infilename = sprintf('data\/%010d.pcd', i);
    data = pcdread(infilename);
    v = [];
    for j = 1 : length(data)
        if data(i, 3) > 3
            v = [v, i];
        end
    end
    data(v, :) = [];
    data(:, 4) = [];
    outfilename = sprintf('data\/%010d.
end